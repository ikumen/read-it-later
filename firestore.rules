rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Block all client access, only use the Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}