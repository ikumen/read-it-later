/**
 * Save given PDF to Firebase Storage as "page.id".pdf.
 *
 * @param {Object} page
 * @param {String} page.id
 * @param {String} page.url
 * @param {Buffer} contents PDF buffer of web page
 */
exports.savePagePDFToStorage = async ({id, url}, contents) => {
  
};
